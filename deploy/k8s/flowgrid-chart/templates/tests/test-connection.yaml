apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flowgrid-chart.fullname" . }}-test-connection"
  labels:
    {{- include "flowgrid-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flowgrid-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
