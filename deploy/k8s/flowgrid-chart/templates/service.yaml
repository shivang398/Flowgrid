apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowgrid-chart.fullname" . }}
  labels:
    {{- include "flowgrid-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flowgrid-chart.selectorLabels" . | nindent 4 }}
